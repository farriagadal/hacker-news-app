import { styled } from 'src/stitches'

export const Footer = styled('footer', {
  background: '#ccc',
  textAlign: 'center',
  padding: '15px 0',
  fontSize: '12px',
  fontWeight: 600,
  a: {
    color: '#1890ff'
  }
})
